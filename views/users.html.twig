{% autoescape false %}
<h2 class="tableHeader">Overzicht gebruikers</h2>
		  
		  <table class="tableToSort">
                    <thead>
                        <th>Rank</th>
                        <th>Nickname</th>
                        <th>Score</th>
                        <th>Achievements</th>
                        <th>Highscores</th>
                    </thead>
                      <tbody>
                          {% for user in users %}
                            {% if loop.index is even %}
                                <tr class="even">
                            {% elseif loop.index is odd %}
                                <tr class="odd">  
                            {% endif %}
                                    <td> # {{ user.rank }}</td>  
                                  <td> 
                                      <a href="{{url}}/index.php/users/{{user.id}}">{{ user.nickname }}</a>
                                  </td>
                                  <td>{{ user.score }}</td>
                                  <td>{{ user.achievements }}</td>
                                  <td>{{ user.highscores}}</td>
                              </tr>
                          {% endfor %}
                      </tbody>
		  </table>{# empty Twig template #}
{% endautoescape %}